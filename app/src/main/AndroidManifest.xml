<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication.mvx">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rxjava.RxJavaOperatorMainActivity"></activity>
        <activity android:name=".rxjava.RxMainActivity" />
        <activity android:name=".TwitterNext.TwitterNextPageActivity" />
        <activity android:name=".stride.ActivityTwitter" />
        <activity android:name=".coin.ui.home.CoinHomeActivity" />
        <activity android:name=".Recycle.RecycleActivity" />
        <activity android:name=".ret.RetActivity" />
        <activity
            android:name=".mvvmRoom.AddNoteActivity"
            android:parentActivityName=".mvvmRoom.RoomActivity" />
        <activity android:name=".mvvmRoom.RoomActivity" />
        <activity android:name=".mvvm.MvvmActivity" />
        <activity android:name=".mvp.MVPActivity" />
        <activity android:name=".mvc.MVCActivity" />
        <activity android:name=".bb.AboutActivity" />
        <activity
            android:name=".ArcActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>